<style>
    body {
        width: 100vw;
        height: 100vh;
        margin: 0;
        background-color: #f4f4f4;
        font-family: 'Arial', sans-serif;
        overflow: hidden;
        /* Empêche le scroll global */
    }

    /* Conteneur principal */
    .principale {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
    }

    /* Icône de menu hamburger */
    .menu-icon {
        position: fixed;
        top: 20px;
        left: 20px;
        width: 35px;
        height: 25px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        cursor: pointer;
        z-index: 1001;
    }

    .bar {
        height: 5px;
        background-color:#ff5722;
        width: 100%;
    }

    /* Menu latéral */
    .containerleft {
        position: fixed;
        top: 0;
        left: -250px;
        /* Masqué par défaut */
        width: 250px;
        height: 100%;
        background-color: transparent;
        color: white;
        padding-top: 30px;
        display: flex;
        flex-direction: column;
        align-items: center;
        box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        transition: left 0.3s ease;
    }
    .containerleft.open {
        left: 0;
        /* Ouvre le menu */
    }
    
    .containerrigth {
        width: 75%;
        height: 100%;
        background-color: transparent;
        color: white;
        font-family: Arial, Helvetica, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 20px;
        overflow-y: auto;
    }

    #centrale {
        width: 100%;
        height: auto;
        background-color: rgb(71, 69, 69);
        border-radius: 15px;
        padding: 15px;
        color: white;
        margin-bottom: 20px;
    }

    li {
        list-style: none;
        background: rgb(66, 108, 179);
        width: 75%;
        padding: 15px;
        margin: 10px 0;
        border-radius: 10px;
        text-align: left;
        color: white;
        font-size: 1.1rem;
        word-wrap: break-word;
        transition: background-color 0.3s ease;
    }

    li:hover {
        background-color: rgb(49, 79, 134);
    }

    form {
        background-color: white;
        width: 100%;
        max-width: 500px;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
        color: #333;
        overflow-y: auto;
    }

    input,
    select,
    textarea {
        width: 100%;
        padding: 15px;
        margin-bottom: 20px;
        border-radius: 10px;
        border: 1px solid #ddd;
        font-size: 1rem;
        transition: all 0.3s ease;
        background-color: #f9f9f9;
    }

    input:focus,
    select:focus,
    textarea:focus {
        border-color: #4d62ea;
        outline: none;
        box-shadow: 0 0 5px rgba(77, 98, 234, 0.6);
    }

    button {
        width: 100%;
        padding: 15px;
        background-color: #ff5722;
        color: white;
        border: none;
        border-radius: 10px;
        font-size: 1.2rem;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    button:hover {
        background-color: #3a4fb0;
    }

    #bouton {
        width: 100%;
        margin-top: 20px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 15px;
    }

    #bouton .btn {
        width: 80%;
        padding: 15px;
        font-size: 1.2rem;
        background-color: transparent;
        color: #ff5722;
        border-radius: 10px;
        text-align: center;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    #bouton .btn:hover {
        background-color: #ff5722;
        color: white;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .containerleft {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            width: 60%;
            height: 100%;
            margin: 0;
            padding-top: 10px;
            background-color: white;
        }

        .containerrigth {
            width: 100%;
            margin-right: 0;
            height: auto;
        }

        form {
            width: 90%;
            max-width: 400px;
        }

        input,
        button {
            width: 90%;
            height: 50px;
            background-color:;
        }

        li {
            width: 100%;
            padding: 15px;
            font-size: 1rem;
        }

        #bouton .btn {
            width: 100%;
            padding: 20px;
            font-size: 1.2rem;
        }
    }
</style>

<div class="principale">
    <!-- Icône du menu hamburger -->
    <div class="menu-icon" id="menu-icon">
        <div class="bar"></div>
        <div class="bar"></div>
        <div class="bar"></div>
    </div>

    <!-- Menu latéral gauche -->
    <div class="containerleft" id="menu">
        
        <div id="bouton">
            <button class="btn" type="button" onclick="getHome()">Home</button>
            <button class="btn" type="button" onclick="getPasswordItem()">Password-item</button>
            <button class="btn" type="button" onclick="getLogin()">Login</button>
            <button class="btn" type="button" onclick="getSignup()">Signup</button>
            <button class="btn" type="button" onclick="getLogout()">Logout</button>
        </div>
    </div>

    <div class="containerrigth">
        <form action="/newpass" class="shaldow" method="post">
            <label for="email">Adresse email</label>
            <input type="email" id="email" name="email" class="input" placeholder="Votre email" required>
            <label for="link">website link</label>
            <input type="text" id="link" name="link" class=" input" placeholder="le lien vers votre..." required>
            <label for="catehgory">category</label>
            <input type="text" id="category" name="category" class="input" required>
            <label for="password">Password</label>
            <input type="password" id="password" name="password" class="input" placeholder="Votre mot de passe"
                required>
            <button type="submit" class="btnn shadow">Save Password</button>
        </form>
    </div>
</div>
<script>
    window.onload = () => {
        // Fonction pour ouvrir/fermer le menu hamburger
        const menuIcon = document.getElementById('menu-icon');
        const sideMenu = document.getElementById('menu');


        menuIcon.addEventListener('click', () => {
            sideMenu.classList.toggle('open'); // Ouvre ou ferme le menu
        });


        // Récupérer la liste des éléments depuis le HTML
        const itemsList = document.querySelectorAll('#items-list li');
        if (itemsList.length === 0) {
            console.log("Aucun élément trouvé !");
        } else {
            console.log(itemsList)
            console.log(`${itemsList.length} éléments trouvés`);
        }
        // Créer une nouvelle liste dynamique
        const dynamicList = document.createElement('ul');
        document.body.appendChild(dynamicList);  // Ajouter la nouvelle liste à la page

        // Parcourir les éléments récupérés pour les afficher dans la nouvelle liste
        itemsList.forEach(item => {
            const li = document.createElement('li');
            li.textContent = "";
            li.textContent = item.textContent;  // Récupérer le texte de chaque item
            dynamicList.appendChild(li);
        });
    };

    function getHome() {
        window.location.href = "/home"
    }
    function getLogin() {
        window.location.href = '/login';
    }
    function getSignup() {
        window.location.href = '/signup';
    }
    function getLogout() {
        window.location.href = '/logout';
    }
    function getPasswordItem() {
        window.location.href = '/listedallpassword';
    }

</script>