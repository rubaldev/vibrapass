<style>
    body {
        width: 100vw;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 16px;
        font-family: Arial, Helvetica, sans-serif;
        background-color: rgba(0, 0, 0, 0.6);
        margin: 0;
    }

    #principale {
        background-color: rgba(0, 0, 0, 0.6);
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100vw;
        height: 100vh;
        padding: 20px;
        border-radius: 10px;
    }

    video {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: -1;
    }

    .formulaire {
        width: 100%;
        max-width: 400px;
        background-color: rgba(0, 0, 0, 0.7);
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.7);
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .form-label {
        font-weight: bold;
        color: #f8f9fa;
        margin-bottom: 10px;
    }

    .form-control {
        border-radius: 10px;
        border: 1px solid #fff;
        padding: 10px;
        margin-top: 5px;
        font-size: 16px;
        width: 100%;
    }

    .btn {
        font-weight: bold;
        padding: 12px 30px;
        border-radius: 30px;
        cursor: pointer;
        transition: all 0.3s ease;
        width: 100%;
        margin-top: 20px;
    }

    .btn-primary {
        background-color: #ff5722;
        color: white;
        border: none;
    }

    .btn-primary:hover {
        background-color: #d13607;
        color: white;
    }
    input[type="password"] {
            border: 2px solid #fff;
            background-color: transparent;
            font-size: 1.1rem;
            color: white;
            margin-bottom: 1rem;
            transition: border-color 0.3s ease;
        }

    input[type="password"]:focus {
            border-color: #ff5722;
            border-width: 3px 0px 0px 3px;
            outline: none;
        }
    .btn-reset {
        background-color: white;
        color: #f1a100;
        border: none;
    }

    .btn-reset:hover {
        background-color: #e6e6e6;
    }

    #message {
        font-size: 1rem;
        font-family: Arial;
        color: rgb(222, 67, 67);
        text-align: center;
        margin-top: 15px;
    }

    .instruction-text {
        color: white;
        text-align: center;
        font-size: 1.2rem;
        margin-bottom: 20px;
    }

    /* Responsive */
    @media (max-width: 768px) {
        .formulaire {
            padding: 20px;
        }

        .form-control {
            font-size: 14px;
        }

        .btn {
            padding: 10px 20px;
        }

        #message {
            font-size: 14px;
        }
    }
</style>

<video autoplay loop muted>
    <source src="/font/6917189_Motion Graphics_Motion Graphic_3840x2160.mp4" type="video/mp4">
    Votre navigateur ne prend pas en charge la vidéo.
</video>

<div id="principale">
    <div class="formulaire">
        <h2 style="color: white;">Réinitialisation du mot de passe</h2>
        <div class="instruction-text">
            <p>Veuillez entrer un nouveau mot de passe ci-dessous. Assurez-vous qu'il soit sécurisé.</p>
        </div>
        <form id="formulaire" action="/otherPassWord" method="post">
            <div>
                <label for="password" class="form-label">Nouveau Mot de Passe</label>
                <input type="password" name="password" id="password" class="form-control"
                    placeholder="Entrez votre nouveau mot de passe" required>
            </div>
            <button type="submit" class="btn btn-primary">Valider</button>
        </form>
        <span id="message">{{message}}</span>
    </div>
</div>